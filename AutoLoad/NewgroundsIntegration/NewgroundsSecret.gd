extends Node
# This file is included in .gitignore so that you don't accidentally
# push it to a public repo.
# To be honest I'm not sure exactly how important it is to keep the app ID secret
# on Newgrounds. I'm playing it safe here, but I don't believe it's actually
# as important as keeping Game Jolt's private key safe.

# Add your app ID here before compiling.
const APP_ID = "51766:dxosIiFu"

func get_app_id():
	return APP_ID
